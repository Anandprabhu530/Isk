import { ScrollView, Text } from "react-native";

const exercise = () => {
  return (
    <ScrollView style={{ paddingVertical: 50 }}>
      <Text style={{ color: "#ffffff" }}>exercise</Text>
    </ScrollView>
  );
};

export default exercise;
